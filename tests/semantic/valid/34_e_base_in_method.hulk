type Person(name:String) {
    name:String = name;
    greet() => "Hello, " @ self.name;
}
type Student(name:String ) inherits Person(name) {
    greet() => base() @ "!";
}
let s = new Student("Alice") in print(s.greet());
